#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=0, x=0, max=0;
    for (i==0; i<3; i++){
        printf("Enter number: ");
        scanf("%d",&x);
        if (x)
        if (x>max){
            max =x;
        }
    }
    printf("Maximum number = %d",max);
    return 0;
}
